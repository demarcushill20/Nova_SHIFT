# Nova SHIFT Environment Variables Example
# Copy this file to .env and fill in your actual values.

# --- Language Model APIs ---
# Required for agent reasoning and generation
OPENAI_API_KEY=
GOOGLE_API_KEY= # Optional: For Gemini models

# --- Long-Term Memory (Pinecone) ---
# Required for persistent knowledge storage and retrieval
PINECONE_API_KEY=
# PINECONE_ENV=
# Specify pod environment for Pinecone Starter (free tier) or Standard plans
# PINECONE_POD_ENVIRONMENT="us-east-1" # Commented out as using Serverless
# Or for Serverless (uncomment and configure if using Serverless):
PINECONE_SERVERLESS_CLOUD=aws # Set based on user info
PINECONE_SERVERLESS_REGION=us-east-1 # Set based on user info
PINECONE_INDEX_NAME=nova-ai-memory # Or your chosen index name

# --- Shared Memory / Messaging (Redis) ---
# Required for inter-agent communication and coordination
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0 # Specify Redis database number (0 is default)
REDIS_PASSWORD="" # Leave empty if no password is set

# --- Tool-Specific API Keys ---
# Add keys required by specific toolkits as needed
TAVILY_API_KEY=# Required for WebSearchToolkit
BRAVE_API_KEY= # Required for BraveSearchToolkit
SMITHERY_API_KEY= # Required for MCP connections via Smithery SDK
PERPLEXITY_API_KEY= # Required for PerplexitySearchToolkit
# Example from RESEARCH.md:
# BING_API_KEY="YOUR_BING_SEARCH_API_KEY"
# Example for a hypothetical weather tool:
# WEATHER_API_KEY="YOUR_WEATHER_API_KEY"

# --- General Configuration ---
# Optional: Set logging level (e.g., DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: Specify the base directory for toolkits if not standard
# TOOLKIT_BASE_DIR="./tools"
